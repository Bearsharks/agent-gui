interface:
  display_name: "Plan GUI MCP"
  short_description: "Review plans through Agent GUI MCP sessions"
  default_prompt: "Use $plan-gui-mcp to create a browser-reviewable Agent GUI plan session and handle feedback revisions."
